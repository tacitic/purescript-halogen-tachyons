purescript-halogen-tachyons
===========================
Simple wrappers for Tachyons classes.

## Building
Make sure Haskell and Stack are installed. To generate the Tachyons modules run `yarn build`.

## Testing
Run `yarn test` to test if compilation works.
